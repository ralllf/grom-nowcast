# Evidence

Each verification run writes a UTC timestamp folder (`YYYYMMDD-HHMMSS`) here. Cleanup must not delete this tree.

A complete run has `doctor.json`, `notes.md`, and `01-` / `02-` / `03-` PNGs. The PR that introduced this skill includes one driven `location-pin` run.
